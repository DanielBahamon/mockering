<% @mockers.each do |mocker| %>
	<div class="col-12 col-md-6 col-lg-4 col-xl-3 pt-3 p-0">
		<%= link_to mocker, class: "link_card card" do %>
			<div class="card-body">
				<div class="row">
					<div class="col-auto p-0">
						<%= image_tag avatar_url(mocker), class: "_avatar _4" %>
					</div>
					<div class="col">
						<div class="d-table fullheight fullwidth">
							<div class="d-table-cell v-middle">
								<div class="row m-0 fullwidth">
									<div class="col p-0">
										<div class="text-ellipsis">
											<p class="_fs10pt m-0 _fs90per _username"><%= mocker.slug %> <%= image_tag badge_icon(mocker), class: "_badge_verification _tooltip _2", "data-toggle": "tooltip","data-placement": "bottom", title: tooltip_badge(mocker) if mocker.verification? %></p>
										</div>
									</div>
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>
		<% end %>
	</div>
<% end %>