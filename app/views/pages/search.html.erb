<% content_for :title, "#{params[:q] if params[:q].present?} - Free content to enjoy" %>
<% content_for :name, "Search" %>

<% content_for :title, "#{params[:q] if params[:q].present?} - Free content to enjoy" %>
<% content_for :name, "Search" %>
<div class="_navy">
	<div class="_cont _1">
		<div class="_back">
			<i class="bi bi-arrow-left _1"></i>
			<input class="_ocult _2" action="action" onclick="window.history.go(-1); return false;" type="submit" value="back" />
		</div>
		<b><%= params[:q] if params[:q].present? %></b>
	</div>
</div>
<div class="_cont _1">
	<%= render 'mockers/cards' %>
</div>
<div class="_cont _2">
  <% @searchmocks.each do |mock| %>
    <article> 
      <%= render 'mocks/mocky', mock: mock %>
    </article>
  <% end %>
</div>
<div class="_cont _3">
	<%= will_paginate @searchmocks %>
</div>
